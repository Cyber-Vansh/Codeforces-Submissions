import math
 
t = int(input())
for _ in range(t):
    a,b = map(int,input().split())
 
    # if b<a:
    #     b,a = a,b
 
    # x = None
 
    # for i in range(int(b**0.5),0,-1):
    #     if b%i==0:
    #         x = max(i,b//i)
    #         break
    
    # g = math.gcd(a,b)
 
    # ans = float('inf')
 
    # for i in range(1,int(g**0.5)+1):
    #     if g%i==0:
    #         curr1 = (i-1)+(a//i) + (x-i)+(b//x)
 
    #         j = g//i
    #         curr2 = (j-1)+(a//j) + (x-j)+(b//x)
 
    #         ans = min(ans,curr1,curr2)
    
    # print(ans)
 
 
    ans = float('inf')
 
    for i in range(1,10**5):
        curr = (i-1) + math.ceil(a/i) + math.ceil(b/i)
 
        ans = min(ans,curr)
    
    print(ans)
 
 
 
 
 
 
 