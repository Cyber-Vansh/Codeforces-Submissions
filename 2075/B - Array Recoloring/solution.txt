t = int(input())
for _ in range(t):
    n,k = map(int,input().split())
    arr = list(map(int,input().split()))
 
    if k == 1:
        print(max(arr[0]+max(arr[1:]),arr[-1]+max(arr[:-1])))
    else:
        arr.sort(reverse=True)
        print(sum(arr[:k+1]))